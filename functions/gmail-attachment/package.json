{
  "name": "gmail-attachment",
  "version": "1.0.0",
  "description": "Gmail添付ファイル取得Lambda関数",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "node tests/manual-run.mjs",
    "setup-oauth": "node tests/setup-oauth.mjs",
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "deploy:fast": "sam build && sam deploy",
    "local": "sam local start-api",
    "logs": "sam logs -n GmailAttachmentFunction --tail"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "googleapis": "^144.0.0"
  }
}
